apiVersion: v1
kind: Service
metadata:
  name: awkwob-service
  labels:
    app: awkwob-devops-project
spec:
  selector:
    app: awkwob-devops-project
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 31200
